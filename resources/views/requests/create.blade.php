/* Icon badge */
.tkt-icon { background: #e8650a; box-shadow: 0 4px 24px rgba(232,101,10,0.28); }

/* Callout icon bg & color */
.tkt-callout-icon { background: #fff4ee; color: #e8650a; }

/* Download button */
.tkt-dl-btn { background: #e8650a; }
.tkt-dl-btn:hover { background: #cf5608; }

/* Input focus ring */
.tkt-input:focus, .tkt-select:focus, .tkt-textarea:focus {
    border-color: #e8650a;
    background: #fff;
    box-shadow: 0 0 0 3px rgba(232,101,10,0.1);
}

/* Upload zone */
.tkt-upload-zone { background: #fff8f4; border-color: #f0c9aa; }
.tkt-upload-zone:hover { background: #fff2e8; border-color: #e8650a; }
.tkt-upload-icon { color: #e8650a; }

/* Submit button */
.tkt-submit { background: #e8650a; box-shadow: 0 4px 20px rgba(232,101,10,0.28); }
.tkt-submit:hover { background: #cf5608; box-shadow: 0 8px 28px rgba(232,101,10,0.32); }
